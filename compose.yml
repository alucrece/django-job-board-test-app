services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:8000"
    env_file: .envlocal
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: p4ssword123!
      POSTGRES_DB: mydatabase
    ports:
      - "5432:5432"